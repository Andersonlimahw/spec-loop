# SpecLoop Docs

Astro/Starlight documentation for SpecLoop.

```bash
npm install
npm run build
```

Local preview:

```bash
npm run dev
```

The main CLI command reference lives in
`src/content/docs/06-cli-and-workflows.md`; examples live in
`src/content/docs/07-examples.md`.

PDF export is intentionally deferred until the CLI demo and docs narrative are stable.
