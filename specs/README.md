# SpecLoop Specs

SpecLoop uses SDD: contract first, implementation second, validation always.

## Index

- [Subplans](subplans/)
- [ADRs](adr/)

## Core Acceptance Criteria

- Specs and schemas are versioned.
- CLI behavior is reproducible.
- Safety defaults are enforced before runtime adapters execute actions.
- Reports are structured enough for agents and readable enough for humans.
